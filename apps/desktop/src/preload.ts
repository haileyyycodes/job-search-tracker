/**
 * Placeholder — Phase 5 exposes a typed `window.electronAPI` surface here via
 * contextBridge, backing packages/shared's ElectronDataSource (thin
 * ipcRenderer.invoke wrapper implementing the DataSource interface).
 */
export {};
